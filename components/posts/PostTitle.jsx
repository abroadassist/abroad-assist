import React from "react";

const PostTitle = ({ title }) => {
  return <h1 className=" text-3xl md:text-4xl lg:text-5xl mt-3 mb-8 font-semibold font-jumbo_heading">{title}</h1>;
};

export default PostTitle;
